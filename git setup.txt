http://git.newegg.org/projects/IMTW/repos/modules/browse

1.
在開發的目錄下
E:\newkit-v1.0.5\src\modules
按右鍵
選Git Bash here


2. 先設定使用者名稱和電子郵件
git config --global user.name "Jeff.C.Lai"
git config --global user.email "Jeff.C.Lai@newegg.com"
git clone http://jl0u@git.newegg.org/scm/imtw/modules.git
cd modules
git flow init -d

3. 打開vscode
File→Open Folder→modules


4. 儲存當前進度
working directory
staging area
repository(local)

You have an empty repository
To get started you will need to run these commands in your terminal.
New to Git? Learn the basic Git commands
Configure Git for the first time

git config --global user.name "Frank.Y.Liao"
git config --global user.email "Frank.Y.Liao@newegg.com"

Working with your repository
I just want to clone this repository
If you want to simply clone this empty repository then run this command in your terminal.

git clone http://fl51@git.newegg.org/scm/imtw/modules.git

My code is ready to be pushed
If you already have code ready to be pushed to this repository then run this in your terminal.
cd existing-project

git init
git add --all
git commit -m "Initial Commit"
git remote add origin http://fl51@git.newegg.org/scm/imtw/nc2.0.git
git push origin master

My code is already tracked by Git
If your code is already tracked by Git then set this repository as your "origin" to push to.

cd existing-project
git remote set-url origin http://fl51@git.newegg.org/scm/imtw/nc2.0.git
git push origin master

All done with the commands?